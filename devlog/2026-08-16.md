# Project Discrepancy Log — Tí Coolture

**Date:** 2026-08-16  
**Focus:** Admin CMS Sprint (SRS Section 5.7)  

---

## 1. Daily Objectives
Today marks the beginning of the Admin CMS sprint. Following the successful separation of the guest views and the implementation of the `/admin_door` backdoor mechanism yesterday, the primary goal for today is to outline and begin executing the requirements specified in **SRS Section 5.7**.

## 2. Admin CMS (Section 5.7) Status Review

Based on the current state of `UserProfile.tsx` (which serves as the Admin Dashboard for `profile?.role === "Admin"`), here is the assessment of the Admin CMS capabilities:

| Feature ID | SRS Specification | Current Implementation State | Discrepancy / Status |
|---|---|---|---|
| **AD-01** | Login (Supabase Auth + 2FA) | Replaced by stealth `/admin_door` search backdoor for V1 per user instruction. | **Intentionally Deviated** |
| **AD-02** | CRUD shop | Deletion timers and hiding toggles exist. Full Create/Update forms missing. | **Partial** |
| **AD-03** | CRUD products (upload, drag/drop) | Moderation and hiding exist. Form for creating products with multi-image drag-and-drop upload is missing. | **Partial** |
| **AD-04** | CRUD blog post (Markdown, preview) | Completely missing. | **Missing** |
| **AD-05** | Route map editor (click/drag pins) | Completely missing. | **Missing** |
| **AD-06** | Hidden Gems management | Missing dedicated management UI. | **Missing** |
| **AD-07** | Toggle shop/product visibility | Implemented via `handleToggleHideStore` and `handleToggleHideProduct`. | **Resolved** |
| **AD-08** | Dashboard analytics | Click stats tab exists but lacks comprehensive visual representation. | **Partial** |
| **AD-09** | Export CSV/Excel | Missing export logic. | **Missing** |
| **AD-10** | Audit logs | `logApprovalActivity` exists but full viewer is limited. | **Partial** |
| **AD-11** | Enter shop monthly reports | Missing. | **Missing** |
| **AD-12** | Edit `site_settings` (carousel rules) | Missing. | **Missing** |
| **AD-13** | CRUD blog topics | Missing. | **Missing** |

## 3. Action Plan
We have formulated a comprehensive implementation plan to tackle the missing CRUD operations, the specialized route map editor, the markdown blog editor, and the various export/settings functionalities. The plan breaks these down into manageable modules (Core CRUD, Media & Markdown, Map & Settings, Analytics & Export) to ensure steady progress throughout the day.
