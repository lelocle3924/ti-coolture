node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
note.txt

# Claude Code local machine settings (absolute paths, per-machine grants)
.claude/settings.local.json

# Playwright MCP scratch output — page snapshots and console logs
.playwright-mcp/

# Shop photography. The raw drop stays out — ~100MB of Instagram-resolution
# JPEG that nothing renders at full size. The optimised WebP under
# public/shop-photos/ is now COMMITTED (28/08): the earlier call was to keep it
# out as an asset, but then nobody who clones the repo can see the catalogue
# with pictures in it, and that is the whole reason the drop was made.
# 6.5MB for 186 images is a fair price for the mock data being viewable.
# Regenerate from the raw drop with: python scripts/build-shop-photos.py
Ảnh up shop/
manifest.json
